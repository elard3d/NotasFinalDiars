@{
    ViewData["Title"] = "Index";
}

<h1>Nota Etiqueta</h1>


<div class="container">
    @*@foreach (var item in ViewBag.Notas)
    {
        <h3>Titulo </h3>@item.nota.tituloNota
        <br />
        <h3>Contenido: </h3>@item.nota.contenidoNota
        <br />
        <h3>Fecha de modificacion: </h3> @item.nota.fechaModicacion.ToShortDateString()
        <br />
        <br /><br />
    }
    <br />
    <a href="/Home/Index">Volver a mostrar todo</a>*@
</div>